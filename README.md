# RainFocus UI Challenge

## Live Demo
You can view the live application here: https://6ym9xz-3000.csb.app/

## How to Run Locally
If you prefer to run the project locally, please follow these steps:
1. Ensure [Node.js](https://nodejs.org/) is installed.
2. Run `npm install` in the project root to install dependencies.
3. Run `npm run build` to compile the project.
4. Run `npm run start` to launch the application.